[
  {"id":"q1","question":"Lệnh SQL nào dùng để LẤY dữ liệu từ bảng?","questionEn":"Which SQL command is used to RETRIEVE data from a table?","options":["INSERT","UPDATE","SELECT","GET"],"optionsEn":["INSERT","UPDATE","SELECT","GET"],"answer":2,"explanation":"SELECT là lệnh truy vấn dữ liệu (read-only). INSERT thêm mới, UPDATE sửa, DELETE xóa. SELECT không thay đổi dữ liệu trong database.","explanationEn":"SELECT is the data query command (read-only). INSERT adds new data, UPDATE modifies it, and DELETE removes it. SELECT does not change any data in the database."},
  {"id":"q2","question":"Mệnh đề WHERE trong SQL dùng để làm gì?","questionEn":"What is the WHERE clause in SQL used for?","options":["Sắp xếp kết quả","Lọc bản ghi theo điều kiện","Giới hạn số cột trả về","Nhóm kết quả theo cột"],"optionsEn":["Sorting results","Filtering records by a condition","Limiting the number of columns returned","Grouping results by a column"],"answer":1,"explanation":"WHERE lọc bản ghi thỏa mãn điều kiện. Ví dụ WHERE age > 25 chỉ trả về user có tuổi lớn hơn 25. Không có WHERE → trả về tất cả.","explanationEn":"WHERE filters the records that satisfy a condition. For example, WHERE age > 25 returns only users older than 25. Without a WHERE clause → all records are returned."},
  {"id":"q3","question":"Lệnh nào SAU ĐÂY nguy hiểm nhất nếu thiếu mệnh đề WHERE?","questionEn":"Which of the following commands is the most dangerous when the WHERE clause is missing?","options":["SELECT * FROM users","INSERT INTO users VALUES(...)","DELETE FROM users","CREATE TABLE users(...)"],"optionsEn":["SELECT * FROM users","INSERT INTO users VALUES(...)","DELETE FROM users","CREATE TABLE users(...)"],"answer":2,"explanation":"DELETE FROM users (thiếu WHERE) xóa TOÀN BỘ dữ liệu trong bảng users. Không thể undo nếu không có backup. Tương tự UPDATE thiếu WHERE sẽ sửa TẤT CẢ bản ghi.","explanationEn":"DELETE FROM users (with no WHERE) deletes ALL data in the users table. It cannot be undone without a backup. Likewise, an UPDATE with no WHERE modifies ALL records."},
  {"id":"q4","question":"SELECT name FROM users WHERE name LIKE 'Nguyen%' trả về gì?","questionEn":"What does SELECT name FROM users WHERE name LIKE 'Nguyen%' return?","options":["User có tên chứa 'Nguyen' ở bất kỳ vị trí nào","User có tên kết thúc bằng 'Nguyen'","User có tên bắt đầu bằng 'Nguyen'","User có đúng tên là 'Nguyen'"],"optionsEn":["Users whose name contains 'Nguyen' anywhere","Users whose name ends with 'Nguyen'","Users whose name starts with 'Nguyen'","Users whose name is exactly 'Nguyen'"],"answer":2,"explanation":"'Nguyen%': % là wildcard khớp với bất kỳ chuỗi nào. Pattern 'X%' = bắt đầu bằng X. '%X' = kết thúc bằng X. '%X%' = chứa X ở bất kỳ đâu.","explanationEn":"'Nguyen%': % is a wildcard that matches any string. The pattern 'X%' = starts with X, '%X' = ends with X, and '%X%' = contains X anywhere."},
  {"id":"q5","question":"INNER JOIN kết hợp dữ liệu như thế nào?","questionEn":"How does an INNER JOIN combine data?","options":["Lấy tất cả bản ghi từ bảng bên trái dù không khớp","Chỉ lấy bản ghi có khớp ở CẢ HAI bảng","Lấy tất cả bản ghi từ cả hai bảng","Chỉ lấy bản ghi không khớp"],"optionsEn":["It returns all rows from the left table even when they do not match","It returns only rows that match in BOTH tables","It returns all rows from both tables","It returns only the rows that do not match"],"answer":1,"explanation":"INNER JOIN = intersection — chỉ lấy bản ghi có giá trị khớp ở cả hai bảng. Nếu orders có user_id=5 nhưng không có user id=5 → order đó không xuất hiện trong INNER JOIN.","explanationEn":"INNER JOIN = the intersection — it returns only rows whose values match in both tables. If orders has user_id=5 but there is no user with id=5 → that order will not appear in the INNER JOIN."},
  {"id":"q6","question":"COUNT(*) trong SQL dùng để làm gì?","questionEn":"What is COUNT(*) used for in SQL?","options":["Đếm số cột","Đếm số bản ghi (rows)","Tính tổng giá trị","Tìm giá trị lớn nhất"],"optionsEn":["Counting the number of columns","Counting the number of records (rows)","Summing values","Finding the maximum value"],"answer":1,"explanation":"COUNT(*) đếm số hàng trong kết quả. SELECT COUNT(*) FROM orders WHERE user_id=1 → đếm số đơn hàng của user 1. COUNT(column) đếm số hàng có giá trị không null trong cột đó.","explanationEn":"COUNT(*) counts the number of rows in the result. SELECT COUNT(*) FROM orders WHERE user_id=1 → counts user 1's orders. COUNT(column) counts the rows that have a non-null value in that column."},
  {"id":"q7","question":"GROUP BY dùng để làm gì?","questionEn":"What is GROUP BY used for?","options":["Lọc bản ghi theo điều kiện","Sắp xếp kết quả","Nhóm bản ghi có cùng giá trị để áp dụng aggregate function","Giới hạn số kết quả"],"optionsEn":["Filtering records by a condition","Sorting results","Grouping records with the same value in order to apply an aggregate function","Limiting the number of results"],"answer":2,"explanation":"GROUP BY gộp các bản ghi có cùng giá trị, thường kết hợp với COUNT, SUM, AVG, MAX, MIN. Ví dụ: GROUP BY city → thống kê theo từng thành phố.","explanationEn":"GROUP BY merges records that share the same value, usually together with COUNT, SUM, AVG, MAX, or MIN. For example: GROUP BY city → statistics per city."},
  {"id":"q8","question":"Lệnh nào cập nhật email của user có id=3?","questionEn":"Which command updates the email of the user with id=3?","options":["UPDATE users SET email='new@mail.com'","UPDATE users SET email='new@mail.com' WHERE id=3","UPDATE SET email='new@mail.com' FROM users WHERE id=3","MODIFY users email='new@mail.com' WHERE id=3"],"optionsEn":["UPDATE users SET email='new@mail.com'","UPDATE users SET email='new@mail.com' WHERE id=3","UPDATE SET email='new@mail.com' FROM users WHERE id=3","MODIFY users email='new@mail.com' WHERE id=3"],"answer":1,"explanation":"Cú pháp: UPDATE [table] SET [column]=[value] WHERE [condition]. Phải có WHERE để chỉ định bản ghi cần cập nhật, nếu không sẽ cập nhật TẤT CẢ bản ghi.","explanationEn":"Syntax: UPDATE [table] SET [column]=[value] WHERE [condition]. A WHERE clause is required to specify which record to update; otherwise ALL records are updated."},
  {"id":"q9","question":"SELECT * FROM products WHERE price BETWEEN 100000 AND 500000 lấy gì?","questionEn":"What does SELECT * FROM products WHERE price BETWEEN 100000 AND 500000 return?","options":["Sản phẩm giá dưới 100,000","Sản phẩm giá trên 500,000","Sản phẩm giá từ 100,000 đến 500,000 (bao gồm cả hai đầu)","Sản phẩm giá đúng 100,000 hoặc 500,000"],"optionsEn":["Products priced below 100,000","Products priced above 500,000","Products priced from 100,000 to 500,000 (both endpoints included)","Products priced at exactly 100,000 or 500,000"],"answer":2,"explanation":"BETWEEN là inclusive (bao gồm cả hai đầu). BETWEEN 100000 AND 500000 = giá >= 100000 AND giá <= 500000.","explanationEn":"BETWEEN is inclusive (both endpoints included). BETWEEN 100000 AND 500000 = price >= 100000 AND price <= 500000."},
  {"id":"q10","question":"LEFT JOIN khác INNER JOIN ở điểm gì?","questionEn":"How does a LEFT JOIN differ from an INNER JOIN?","options":["LEFT JOIN chỉ lấy bảng bên trái","LEFT JOIN lấy tất cả bản ghi từ bảng trái, kể cả khi không có khớp ở bảng phải","LEFT JOIN nhanh hơn INNER JOIN","Chúng hoàn toàn giống nhau"],"optionsEn":["LEFT JOIN returns only the left table","LEFT JOIN returns all rows from the left table, even when there is no match in the right table","LEFT JOIN is faster than INNER JOIN","They are exactly the same"],"answer":1,"explanation":"LEFT JOIN: tất cả bảng trái + bản ghi khớp bảng phải (không khớp → NULL). Ví dụ: LEFT JOIN users ON orders.user_id = users.id → lấy tất cả orders, kể cả orders chưa có user.","explanationEn":"LEFT JOIN: all rows from the left table + matching rows from the right (no match → NULL). For example: LEFT JOIN users ON orders.user_id = users.id → returns all orders, including orders that have no user."},
  {"id":"q11","question":"SELECT user_id, SUM(amount) FROM orders GROUP BY user_id trả về gì?","questionEn":"What does SELECT user_id, SUM(amount) FROM orders GROUP BY user_id return?","options":["Tổng tất cả đơn hàng","Danh sách đơn hàng sắp xếp theo user","Tổng giá trị đơn hàng của từng user","Số lượng đơn hàng của từng user"],"optionsEn":["The total of all orders","A list of orders sorted by user","The total order value for each user","The number of orders for each user"],"answer":2,"explanation":"GROUP BY user_id nhóm theo từng user. SUM(amount) tính tổng amount trong mỗi nhóm. Kết quả: mỗi hàng là một user với tổng tiền đơn hàng của họ.","explanationEn":"GROUP BY user_id groups by each user. SUM(amount) computes the total amount within each group. The result: each row is one user with the total value of their orders."},
  {"id":"q12","question":"IS NULL trong SQL dùng để kiểm tra điều gì?","questionEn":"What does IS NULL check for in SQL?","options":["Giá trị bằng 0","Cột có giá trị rỗng (null) — chưa được nhập","Cột có chuỗi trống ('')","Giá trị âm"],"optionsEn":["A value equal to 0","A column with an empty value (null) — not yet entered","A column with an empty string ('')","A negative value"],"answer":1,"explanation":"NULL trong SQL là giá trị đặc biệt 'không xác định'. Không thể dùng = NULL hay != NULL — phải dùng IS NULL hoặc IS NOT NULL.","explanationEn":"NULL in SQL is a special 'unknown' value. You cannot use = NULL or != NULL — you must use IS NULL or IS NOT NULL."},
  {"id":"q13","question":"ORDER BY name DESC có nghĩa là gì?","questionEn":"What does ORDER BY name DESC mean?","options":["Sắp xếp theo tên, tăng dần A→Z","Sắp xếp theo tên, giảm dần Z→A","Xóa bản ghi theo tên","Nhóm theo tên"],"optionsEn":["Sort by name in ascending order A→Z","Sort by name in descending order Z→A","Delete records by name","Group by name"],"answer":1,"explanation":"ORDER BY sắp xếp kết quả. ASC = ascending (tăng dần, A→Z, 1→9). DESC = descending (giảm dần, Z→A, 9→1). Mặc định là ASC nếu không ghi.","explanationEn":"ORDER BY sorts the results. ASC = ascending (A→Z, 1→9). DESC = descending (Z→A, 9→1). The default is ASC if not specified."},
  {"id":"q14","question":"Lệnh SELECT * FROM users WHERE city IN ('Hanoi', 'HCM') làm gì?","questionEn":"What does SELECT * FROM users WHERE city IN ('Hanoi', 'HCM') do?","options":["Lấy user không ở Hanoi và HCM","Lấy user ở Hanoi HOẶC HCM","Lấy user ở cả Hanoi VÀ HCM","Xóa user ở Hanoi và HCM"],"optionsEn":["Return users not in Hanoi or HCM","Return users in Hanoi OR HCM","Return users in both Hanoi AND HCM","Delete users in Hanoi and HCM"],"answer":1,"explanation":"IN kiểm tra giá trị có trong danh sách không. city IN ('Hanoi', 'HCM') = city = 'Hanoi' OR city = 'HCM'. Tiện hơn viết nhiều OR.","explanationEn":"IN checks whether a value is in a list. city IN ('Hanoi', 'HCM') = city = 'Hanoi' OR city = 'HCM'. It is more convenient than writing several ORs."},
  {"id":"q15","question":"HAVING khác WHERE ở điểm gì?","questionEn":"How does HAVING differ from WHERE?","options":["HAVING dùng trước GROUP BY, WHERE dùng sau","HAVING lọc sau khi GROUP BY (lọc trên aggregate), WHERE lọc trước khi group","Chúng hoàn toàn giống nhau","HAVING chỉ dùng với DELETE"],"optionsEn":["HAVING is used before GROUP BY, WHERE is used after","HAVING filters after GROUP BY (filtering on aggregates), while WHERE filters before grouping","They are exactly the same","HAVING is only used with DELETE"],"answer":1,"explanation":"WHERE lọc bản ghi trước khi group. HAVING lọc các nhóm sau GROUP BY. Ví dụ: GROUP BY user_id HAVING COUNT(*) > 5 → chỉ lấy user có hơn 5 đơn hàng.","explanationEn":"WHERE filters records before grouping. HAVING filters the groups after GROUP BY. For example: GROUP BY user_id HAVING COUNT(*) > 5 → returns only users with more than 5 orders."},
  {"id":"q16","question":"Trong SQL, `AS` dùng để làm gì? Ví dụ: SUM(amount) AS doanh_thu","questionEn":"What is `AS` used for in SQL? Example: SUM(amount) AS revenue","options":["Sắp xếp kết quả","Đặt tên (bí danh) dễ đọc cho cột kết quả","Lọc theo điều kiện","Nối hai bảng"],"optionsEn":["Sort the results","Give a readable name (alias) to a result column","Filter by condition","Join two tables"],"answer":1,"explanation":"AS đặt bí danh (alias) cho cột hoặc bảng, giúp kết quả báo cáo dễ đọc hơn. SUM(amount) AS doanh_thu sẽ hiển thị tên cột là 'doanh_thu'.","explanationEn":"AS gives an alias to a column or table, making report output easier to read. SUM(amount) AS revenue shows the column header as 'revenue'."},
  {"id":"q17","question":"COUNT(DISTINCT user_id) FROM orders trả về gì?","questionEn":"What does COUNT(DISTINCT user_id) FROM orders return?","options":["Tổng số đơn hàng","Số lượng user KHÁC NHAU đã từng đặt hàng","Tổng tiền của tất cả đơn","Số đơn của user đầu tiên"],"optionsEn":["The total number of orders","The number of DISTINCT users who have placed an order","The total amount of all orders","The order count of the first user"],"answer":1,"explanation":"DISTINCT loại bỏ giá trị trùng trước khi đếm. Một user đặt 10 đơn chỉ được tính 1 lần → kết quả là số khách hàng khác nhau, không phải số đơn.","explanationEn":"DISTINCT removes duplicate values before counting. A user with 10 orders is counted once → the result is the number of distinct customers, not the number of orders."},
  {"id":"q18","question":"Để làm báo cáo 'doanh thu theo tháng', bạn cần kết hợp những mệnh đề nào?","questionEn":"To build a 'revenue by month' report, which clauses do you combine?","options":["Chỉ WHERE","GROUP BY (theo tháng) + SUM(amount)","DELETE + WHERE","INSERT + VALUES"],"optionsEn":["Only WHERE","GROUP BY (by month) + SUM(amount)","DELETE + WHERE","INSERT + VALUES"],"answer":1,"explanation":"Báo cáo tổng hợp cần GROUP BY để gộp theo tháng và hàm tổng hợp SUM(amount) để cộng doanh thu mỗi nhóm. Thêm ORDER BY để sắp xếp theo tháng.","explanationEn":"An aggregated report needs GROUP BY to group by month and the aggregate SUM(amount) to add up revenue per group. Add ORDER BY to sort by month."},
  {"id":"q19","question":"Khi BA cần chạy các câu SELECT để lấy số liệu báo cáo, quyền truy cập an toàn nhất nên là gì?","questionEn":"When a BA needs to run SELECT queries to pull report numbers, what is the safest access level?","options":["Toàn quyền quản trị (admin)","Quyền chỉ đọc (read-only)","Quyền xóa bảng","Không cần quyền gì"],"optionsEn":["Full admin rights","Read-only access","Permission to drop tables","No permissions needed"],"answer":1,"explanation":"Quyền read-only cho phép chạy SELECT để lấy dữ liệu nhưng không thể UPDATE/DELETE/DROP — tránh rủi ro lỡ tay làm hỏng dữ liệu thật.","explanationEn":"Read-only access lets you run SELECT to fetch data but cannot UPDATE/DELETE/DROP — avoiding the risk of accidentally damaging real data."}
]
