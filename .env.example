# This app is fully public and static — no environment variables are required.
# Content is file-based (knowledge-content/) and the SQL-practice word bank is a
# committed SQLite file (data/word-bank.db). Nothing to configure.
